# Base URL of the Twind API (no trailing slash). The app appends /v1.
EXPO_PUBLIC_API_URL=http://localhost:8000

# Stripe publishable key (pk_test_... or pk_live_...). Leave empty to run the app
# without payments; the checkout screen will explain that payments are disabled.
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=
